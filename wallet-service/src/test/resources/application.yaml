spring:
  cloud:
    config:
      enabled:
          false
  liquibase:
    change-log: classpath:db/changelog/changelog-master.xml
  jpa:
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: none
